## LoanMe

This repository has its own documentation on what are the requirements and how to run the both applications.

See the `README.md` files in the respective folders (`Backend` and `Frontend`) for instructions.